import { paths } from "../paths";

import "./Main.scss";

const Main = () => {
  return <div className="main">Main</div>;
};

export default {
  path: paths.main,
  element: <Main />,
  children: []
};
